import config from "@/config"
import StaffLayout from "@/layouts/StaffLayout"

const StaffRouter = () => {
    return <StaffLayout/>
}

const StaffRoutes = {
    element: <StaffRouter/>,
    children: [
        
    ]
}

export default StaffRoutes;